{
  "name": "matchia",
  "private": true,
  "version": "1.0.0",
  "description": "match.IA — roda o front-end estático e o back-end Arkitetum.AI juntos, com um comando só.",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev --prefix backend\" \"python serve.py 8080\"",
    "install:all": "npm install --prefix backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
